<%= form_for(@user_favorite) do |f| %>
  <% if @user_favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_favorite.errors.count, "error") %> prohibited this user_favorite from being saved:</h2>

      <ul>
      <% @user_favorite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
