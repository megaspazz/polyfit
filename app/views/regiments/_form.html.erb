<%= form_for(@regiment) do |f| %>
  <% if @regiment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regiment.errors.count, "error") %> prohibited this regiment from being saved:</h2>

      <ul>
      <% @regiment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>

  <%= f.select :exercises, Exercise.all.collect {|x| [x.name, x.id, selected: @regiment.exercises.include?(x)]}, {}, multiple: true %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
